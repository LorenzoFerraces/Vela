# Vela FastAPI base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# Optional: where Vite proxies /api in dev (HTTP + WebSocket). Default 127.0.0.1:8000.
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
