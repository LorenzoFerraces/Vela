---
globs: "['**/*.tsx', '**/*.ts']"
description: Ensures mandatory test coverage for all newly implemented
  functionality to maintain code quality.
alwaysApply: true
---

Write unit and/or integration tests for all new features, components, and API logic. Never commit code that introduces a feature without corresponding test coverage. Use the appropriate testing framework (e.g., Jest, React Testing Library) consistent with existing project patterns.