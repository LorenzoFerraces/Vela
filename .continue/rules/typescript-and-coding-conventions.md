---
globs: "['**/*.tsx', '**/*.ts']"
description: Mandates the use of TypeScript for new files and adherence to
  established project naming standards and type usage.
alwaysApply: true
---

All new files must be written using TypeScript (`.ts` or `.tsx`). Follow existing local naming conventions (e.g., PascalCase for Components, camelCase for variables). All functions and components must utilize explicit type declarations whenever possible to maintain code safety and readability.